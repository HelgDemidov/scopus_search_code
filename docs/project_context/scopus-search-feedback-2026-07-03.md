# Обратная связь по проекту Scopus Search — 2026-07-03

Фидбэк по итогам реализации Table Builder + Journal Landscape Scatter (/explore),
а также по проекту в целом — как тренажёру навыков и демонстрационному кейсу
для работодателей (по стандартам июля 2026).

## Обновление статуса (после 2026-07-03)

Ниже оценки по-прежнему актуальны — коротко, что добавилось с момента написания
и почему это не закрывает ни один из перечисленных пробелов:

- **Personal search data + Explore Personal Redesign** (PR #45/#46) — единый
  источник данных для персональной аналитики (`/stats/personal`,
  `/stats/personal/activity`), retention `search_history`, PersonalActivityChart +
  FilterFingerprintStrip. Качественная фича, но не про AI/observability/нагрузку/a11y.
- **Два новых прод-бага, найденных и починенных** — усиливают п.2 ниже
  («инженерные истории не вынесены наружу»), готовые кандидаты в
  README/Lessons-learned: (a) `select(literal(1)).join()` без `select_from()`
  ронял 500 на любой личной статье в `/profile` — мокнутый тест давал ложное
  покрытие, баг нашли через repro-скрипт напрямую против прод-БД; (b) поиски
  с 0 результатов не попадали в историю/квоту из-за раннего `return []` до
  записи в `search_history`.
- **GC статей-сирот** (issue #47) — лёгкий GitHub Issue вместо полного
  spec.md, реализовано прямым коммитом в main. Сигнал прагматичного
  тикетирования, но сама фича — data hygiene, новый пробел не закрывает.

## Общая оценка

Инженерно это заметно выше типичного «портфолио»-проекта — но как демонстрационный
кейс для работодателя в середине 2026-го ему не хватает одного явного акцента:
применения LLM/AI к собственному домену. Ниже — по пунктам, без реверансов.

## Что реально сильно (не для галочки)

**Backend-дисциплина выше среднего для соло-проекта:**
- Слоистая архитектура interfaces→services→infrastructure→routers выдержана
  последовательно, не декларативно — это редкость, обычно к 3-му месяцу проекта
  слои размываются.
- Auth сделан по-взрослому: AT только in-memory, RT-ротация с piggyback-очисткой
  стухших токенов при каждом рефреше, password reset с revoke-all-tokens после
  смены пароля. Это не «два месяца туториала», а реальное понимание threat model.
- Найденный и починенный баг с cache namespace (prod/staging делили один
  Redis-ключ) — отличная «военная история» для собеседования: обнаружение →
  root cause → фикс → регрессионный тест. Именно такие кейсы, а не список
  технологий, продают senior-мышление.
- CI-матрица (unit/PG16/alembic drift check/coverage gate/lint/e2e на живом
  staging) — это уровень небольшой продуктовой команды, не пет-проекта.

**Сегодняшняя работа (Table Builder + Journal Landscape Scatter) — качественная
инженерия:**
- Whitelist из C(5,2)=10 пар измерений с server-side валидацией + client-side
  логикой «просто исключить выбранное» без дублирования whitelist — элегантное
  решение, видно, что думали о инвариантах, а не просто «сделали, чтобы работало».
- Отдельная забота о non-cached vs cached эндпоинтах, marginal totals vs grand
  total в pivot — не очевидные UX-ловушки, которые вы явно предвидели и объяснили
  пользователю в UI (сноска про totals).
- Scatter-чарт с квадрантами и halo-эффектами — это единственный по-настоящему
  запоминающийся визуальный элемент во всём /explore. Остальное — компетентный,
  но стандартный SaaS-dashboard набор (KPI-тайлы, bar/donut/line).

## Где не дотягивает до стандартов июля 2026

**1. Отсутствие AI-слоя — самый большой пробел.** У вас на руках корпус из
122k статей с abstract'ами, а поиск — чисто CQL/keyword. В середине 2026 для
домена «поиск и анализ научных публикаций» отсутствие семантического поиска
(embeddings + vector search) или хотя бы одной LLM-фичи (авто-саммари тренда,
natural-language → pivot-запрос типа «покажи open access тренд по Германии
с 2020») читается работодателем как «не трогал этот стек руками». Это не
украшение ради моды — это ровно тот кейс, где RAG действительно уместен по
сути задачи, а не притянут искусственно.

**2. Инженерные истории не вынесены наружу.** Баг с cache namespace,
whitelist-паттерн, RT cleanup piggyback — всё это живёт в CLAUDE.md и коммитах,
которые рекрутер никогда не откроет. Если в README нет секции вида
«Engineering decisions / Lessons learned» с 3-5 такими кейсами в формате
«проблема → решение → чему научился» — вы теряете 80% ценности уже проделанной
работы просто из-за упаковки.

**3. Observability на уровне «Railway логи», а не приложения.** Нет
структурированного логирования, трейсинга (OpenTelemetry) или Sentry.
Учитывая, что CI-дисциплина уже на высоком уровне, это дешёвое дополнение
(несколько часов) с непропорционально большим сигналом «понимает production».

**4. Нет истории производительности/нагрузки.** Ни одного числа вида «P95
латентность X мс на Y RPS» или синтетического нагрузочного теста. При корпусе
в 122k статей это не про масштаб, а про то, что вы ни разу не измерили —
а измерить и написать 2 абзаца в README дёшево.

**5. A11y проверяется вручную, не автоматически.** Атрибуты (aria-sort, role,
фокус-кольца) поставлены осознанно — это плюс. Но нет lighthouse-CI/axe-core
джобы, которая бы это закрепляла — учитывая, что автоматизировано уже всё
остальное (lint/coverage/alembic drift), это единственный «ручной» угол QA.

**6. Визуальная идентичность — компетентно, но не отличимо.** Navy/slate/blue
KPI-дашборд с donut/bar/line — таких тысячи на Dribbble и в туториалах shadcn.
Scatter с квадрантами — ваш единственный по-настоящему авторский элемент; стоит
инвестировать ещё в 1-2 таких же нестандартных визуализации, а не в шестой bar
chart.

## Приоритизация, если время ограничено

1. **README как case study** — бесплатно с точки зрения кода, самый высокий ROI
   для работодателя.
2. **Одна AI-фича** — даже простая (embeddings для semantic search по
   названиям/abstract'ам через pgvector, или LLM-суммари выбранного pivot-среза)
   закрывает самый заметный пробел.
3. **Sentry/OTel** — несколько часов, сильный сигнал зрелости.
4. Остальное (нагрузочное тестирование, a11y-автоматизация, доп. визуализации) —
   по вкусу, уже не критично.

## Как тренажёр навыков

Если оценивать не «что покажет работодателю», а «чему вы лично научились» —
тут всё честно отработано: SOLID-слоение, async SQLAlchemy, JWT/refresh-token
security, кэширование с TTL и namespace-изоляцией, i18n с CLDR-плюрализацией на
3 локалях, CI/CD с полной матрицей джобов, dataviz на Recharts (включая
нестандартные вещи типа halo-градиентов и лог-шкалы). Следующий уровень
прокачки, которого пока не было в проекте: работа с LLM API (streaming, tool
use, embeddings/vector DB), базовый observability-стек, нагрузочное
тестирование. Именно эти три темы сейчас определяют разницу между «умеет
строить CRUD с авторизацией» и «умеет строить то, что нанимают строить в 2026».

## Реализация приоритетных задач

### Observability — без внешних сервисов (2026-07-06)

Первая версия плана предполагала Sentry (backend + frontend). При разборе
всплыли реальные развилки, а не просто детали реализации: Sentry требует
создания SaaS-аккаунта (внешнее действие, которое нельзя сделать не глядя),
решения — один Sentry-проект на prod/staging с тегом `environment` или два
раздельных (прямая параллель с прошлым багом namespace-коллизии в
Redis-кэше, PR #32 — то же самое «прод и staging делят один ресурс»), явной
PII-политики (`send_default_pii`), а для фронтенда — отдельного CI-шага с
source maps, без которого стектрейсы бесполезны.

Решение: закрыть пробел без единого нового внешнего сервиса — это снимает
все три развилки разом и не создаёт новых аккаунтов/секретов/окружений.

**Что делаем (~3-4ч):**
1. Structured JSON logging (`structlog`) + middleware с `request_id`
   (contextvars) → тот же id в заголовке ответа `X-Request-ID`.
2. Global exception handler — необработанные исключения логируются как
   structured JSON (ERROR) с полным traceback + request-контекстом.
3. `ping()` в `UpstashRedisClient` (`app/infrastructure/redis_client.py`,
   сейчас только `get`/`setex`) + эндпоинт `/health/redis` — отличает
   «процесс жив» от «процесс жив, но Redis лежит» (`/health/db` уже есть).
4. Алертинг piggyback на существующий 2-часовой seeder-cron: в `seed_db.py`,
   рядом с вызовом `/seeder/gc`, проверка `/health`, `/health/db`,
   `/health/redis`; при не-200 — письмо через уже существующий
   `BrevoEmailService` (`IEmailService`).
5. Разовый расчёт p95 latency из structured-логов за сутки → 2 абзаца в
   README (заодно частично закрывает п.4 «нет истории производительности»).

**Осознанно не делаем** (не полдела, а явный trade-off, стоит называть в
README как есть, а не подавать как «observability полностью закрыта»):
дедупликация/группировка ошибок (одна exception = N строк лога, не «1 issue,
N раз»), задержка алертинга до 2ч (привязана к циклу cron, не realtime),
видимость ошибок фронтенда (self-built версия — по сути переизобретение
куска Sentry), retention логов ограничен окном самого Railway.

Тикет: issue #48.

**Статус (2026-07-06): реализовано**, ветка `feat/observability` смёрджена в
`main` напрямую (без PR — CI на ветке уже был зелёным), коммиты `ad00dd7` +
merge `241302f`.

Пункты 1–4 из плана выше закрыты полностью:
- `structlog` (JSON) + `RequestIDMiddleware` (`app/core/logging_config.py`) —
  `request_id` через contextvars, тот же id и в заголовке ответа
  `X-Request-ID`, и в каждой строке лога этого запроса.
- Global exception handler (`app/main.py`) — необработанные исключения
  логируются как structured ERROR с полным traceback; клиенту уходит только
  generic `{"detail": "Internal server error"}`, без утечки деталей.
- `UpstashRedisClient.ping()` + `GET /health/redis` (дополняет уже
  существовавшие `/health` и `/health/db`) — различает «процесс жив» и
  «процесс жив, но Redis лежит».
- `POST /seeder/health-check` — piggyback на seeder cron (тот же
  `X-Seeder-Secret`, что и `/seeder/gc`): проверяет БД (`SELECT 1`) и Redis
  (`ping()`) in-process, при деградации шлёт письмо через новый метод
  `BrevoEmailService.send_alert_email()` (добавлен в `IEmailService`).
- Тесты: `tests/integration/test_health.py` (5 шт.), `test_observability.py`
  (3 шт.), плюс 5 новых в `test_seeder_endpoint.py` — все на SQLite, без
  `requires_pg`.
- Побочная находка и фикс по ходу: у 500-ответов не проставлялся
  `X-Request-ID` — голый `Exception`-handler у Starlette выполняется в
  `ServerErrorMiddleware`, который стоит выше `RequestIDMiddleware` в стеке
  мидлварей, поэтому код после `call_next()` в мидлваре для этого пути не
  успевает отработать; `request_id` теперь читается напрямую из
  `structlog.contextvars` внутри самого exception handler'а.

Пункт 5 (расчёт p95 latency в README) не реализован — требует накопленной
live-статистики с продакшна, это разовая задача на будущее и не блокирует
закрытие тикета.

**Суть изменений упрощенно.** Раньше, если на сервере что-то ломалось,
единственным способом узнать об этом было вручную зайти в Railway и читать
сырые текстовые логи — без всякой гарантии, что кто-то вовремя туда
заглянет. Теперь у приложения появилась своя «нервная система»:

1. У каждого запроса к серверу теперь есть уникальный номер, который виден
   и клиенту, и во всех связанных с ним строках лога — можно за секунды
   найти именно то, что произошло с конкретным запросом конкретного
   пользователя, а не листать тысячи безликих строк в поисках нужной.
2. Любая непредвиденная ошибка сервера теперь автоматически и подробно
   записывается в аккуратном машиночитаемом формате (JSON), а не свободным
   текстом — это значит, что логи можно в будущем без переписывания кода
   подключить к любой системе анализа или фильтрации.
3. Появилась отдельная проверка «жив ли Redis-кэш», а не только «жива ли
   база данных» — раньше эта часть системы была невидима для мониторинга.
4. Раз в 2 часа, когда и так уже запускается сборщик данных (сидер),
   система заодно тихо проверяет здоровье БД и Redis — и если что-то не в
   порядке, сама отправляет письмо на почту, вместо того чтобы полагаться
   на то, что проблему случайно заметят в логах.

Итог: проект теперь умеет сам сигнализировать «у меня проблема», а не молча
ждать, пока это заметит человек. Осознанно не стали подключать платные
внешние сервисы вроде Sentry — сделали урезанную, но полностью бесплатную
версию той же идеи своими силами (см. компромиссы выше).

**Верификация в проде (2026-07-04, ручной запуск сидера владельцем):**
прогон `workflow_dispatch` (run `28717613226`, 12m45s) подтвердил обе
новые фичи на живом Railway — GC: `Удалено статей-сирот: 0` (в этом цикле
сирот не появилось), health-check: `Всё в порядке` (письмо не
отправлялось — деградации не было). Адрес для алертов (`FROM_EMAIL`)
проверен владельцем — корректный.

**Закрыто:** issue #48.

**Известный пробел → кандидат в следующий тикет.** `X-Request-ID` сейчас
живёт только в HTTP-заголовке ответа — фронтенд нигде не показывает его
пользователю при ошибке (нет ни блока «Error ID: xyz», ни его копирования
в error-toast). Механизм корреляции запрос→лог пока полезен только самому
владельцу при отладке через DevTools/Railway, но не работает как канал
обратной связи от реальных пользователей о конкретных ошибках. Следующий
тикет: фронтенд ловит 5xx-ответы и показывает пользователю короткий
error-id (из `X-Request-ID`) с просьбой прислать его в поддержку — это
замыкает петлю «юзер увидел ошибку → написал → нашли конкретный traceback
по id за секунды». Без этого наблюдаемость остаётся односторонней
(сервер→владелец), а не (пользователь→владелец).

### Нагрузочное тестирование и автоматизация A11y (План действий)

По итогам ревизии текущей кодовой базы (июль 2026), инфраструктура проекта уже обладает высокой степенью зрелости:
- Бэкенд-тесты централизованы в `/tests` (разделены на `unit` и `integration`) и используют `pytest`.
- Фронтенд-тесты (`vitest`) используют co-location в `frontend/src` и интегрированы в жесткий CI-пайплайн (`frontend-tests.yml`).
- Настроен линтинг как для фронтенда (`eslint` в flat-формате `eslint.config.js`), так и для бэкенда (`ruff`, `mypy`).

Ниже представлено оптимизированное Техническое Задание (ТЗ) на закрытие пунктов 4 и 5 из обратной связи, использующее исключительно уже внедренные паттерны.

#### Шаг 1. Сдвиг проверок A11y влево (Shift-Left)
**Цель:** Автоматизировать A11y-проверки без создания новых CI-джобов.
**Действия:**
1. **Установка:** `npm i -D eslint-plugin-jsx-a11y vitest-axe`.
2. **Линтинг (статика):** Интегрировать `eslint-plugin-jsx-a11y` в `frontend/eslint.config.js` (с учетом нового flat-config формата). Это мгновенно добавит A11y-проверки в существующий `npm run lint`.
3. **Тесты (динамика):** Расширить `frontend/src/test/setup.ts` матчерами `vitest-axe`. Написать 1-2 тестовых кейса для критичного UI (например, `TableBuilder`), используя утверждение `expect(await axe(container)).toHaveNoViolations()`, что позволит ловить ошибки рендера в джобе `unit`.

#### Шаг 2. Baseline нагрузочного тестирования
**Цель:** Создать воспроизводимую историю производительности (baseline) для демонстрации работодателю.
**Действия:**
1. **Инструментарий:** Использовать `k6` (наиболее современный инструмент, не требующий тяжелого Python-окружения вроде Locust). Скрипт разместить в `tests/load/baseline.js`.
2. **Сценарий:** Написать скрипт, симулирующий базовую нагрузку на 2-3 критичных read-only эндпоинта (полнотекстовый поиск `/api/articles` и выдача графиков `/api/stats/journal-landscape`).
3. **Документирование:** Запустить скрипт локально против сидированной БД (122k записей). Извлечь показатели P95, P99 и RPS, после чего описать результаты (включая команду запуска) в секции `Engineering decisions / Lessons learned` в `README.md`.

### Нагрузочное тестирование и автоматизация A11y — реализация, сбой и восстановление (2026-07-08 — 2026-07-09)

**Первая попытка (Agy CLI, ветка `feat/a11y-load-test`, 2026-07-08).** ТЗ из раздела выше реализовано
почти дословно за 5 коммитов (`c4c04f7` → `5ab811b`): триггеры CI на ветку, `eslint-plugin-jsx-a11y` +
`vitest-axe`, фикс найденных a11y-нарушений (`ChartCard`, `PivotTable.SortableHeader`, `pagination.tsx`,
`input-group.tsx`), скрипт `tests/load/baseline.js` на k6 + числа в README, финальный коммит с попыткой
починить TS-ошибку типов `vitest-axe`. По итогам последнего коммита **все** фронтенд CI-джобы и деплой
Vercel красные.

**Критический разбор подхода (Claude, 2026-07-09, до вмешательства в код).** Категории инструментов
выбраны верно (статика + рантайм-a11y, k6 без тяжёлого окружения, read-only эндпоинты без JWT/квоты
Scopus для нагрузочного теста), решение не заводить отдельную CI-джобу — соответствует уже сложившемуся
в проекте паттерну piggyback (GC/health-check на seeder-cron и т.п.). Но по существу — два независимых
провала:
1. **A11y:** не проверена совместимость `eslint-plugin-jsx-a11y@6.10.2` (последний релиз, окт. 2024,
   `peerDependencies: eslint@^3...^9`) с уже закреплённым в проекте `eslint@^10.5.0` — проверяется одной
   командой (`npm view ... peerDependencies`) до начала работы. Также не назван явно (в отличие от
   ТЗ Observability, которое честно называло trade-off'ы) blind spot: axe-core в jsdom не может
   проверить color-contrast без Canvas 2D (нет пакета `canvas`).
2. **Нагрузочное тестирование:** методология сломана сильнее, чем сама реализация CI. Скрипт слал
   `?q=machine+learning`, а параметр эндпоинта — `search` (`app/routers/articles.py:179`) — тест ни разу
   не проверял ILIKE-фильтрацию. Хуже: не устранены посторонние переменные перед измерением —
   `create_async_engine(..., echo=True)` (синхронное логирование каждого SQL на каждый запрос),
   отсутствие явного `pool_size`/`max_overflow` (дефолт SQLAlchemy 5+10=15 соединений на 20 VU), и —
   по сложившейся в проекте локальной конвенции — `DATABASE_URL` указывал на боевой Supabase, а не на
   изолированную БД. Итог — опубликованные в README `P(95)=23.07с`, `P(99)=26.05с`, `17.36%` ошибок
   почти наверняка измеряли собственные артефакты замера, а не реальную ёмкость приложения; публикация
   таких цифр для работодателя контрпродуктивна — читается как «падает на 20 пользователях».

**Что именно сломалось — по коммитам:**

| Коммит (Agy) | Итог |
|---|---|
| `af707b4` install jsx-a11y+vitest-axe | 💥 корневая причина: `npm ci` (чистый worktree) падает с `ERESOLVE` на установке — ломает все 5 джоб `frontend-tests.yml` и деплой Vercel одновременно (оба используют `npm ci`) |
| `c3d3b4f` fix a11y warnings | частично хорошо: `ChartCard`/`PivotTable` — правильный семантический фикс (div/span role="button" → настоящий `<button>`); `pagination.tsx` — косметика, добавили `{props.children}`, не тронув настоящую проблему компонента (`<a>` без `href`, не фокусируется как интерактивный элемент — это уже отмечено комментарием в `PivotTable.test.tsx`) |
| `5ab811b` fix(build) | чинит `tsc -b`/`vite build`, но добавленный `vitest.d.ts` нарушает собственные строгие правила ESLint проекта (`no-explicit-any`, `no-empty-object-type`, `no-unused-vars`, `--max-warnings 0`) — `npm run lint` падает даже после фикса установки |

После обхода обеих проблем в чистом worktree: `tsc --noEmit` ✅, `vite build` ✅, `vitest run` — 637/637.
Сама фича (a11y-фиксы + axe-тест + k6-скрипт) в основе рабочая — ветку утопили упаковка зависимостей и
один небрежный follow-up файл, а не архитектура.

**Исправление (Claude, 2026-07-09, та же ветка `feat/a11y-load-test`, 5 коммитов):**
1. `8a1049a` fix(deps) — `frontend/.npmrc` с `legacy-peer-deps=true` (точечный override через
   `package.json` не сработал — это фича pnpm/yarn, не npm). `npm ci` больше не падает.
2. `44f990c` fix(a11y) — точечный eslint-override на `src/vitest.d.ts` (`allowInterfaces:
   'with-single-extends'` — штатная опция правила именно под паттерн declaration merging). `npm run
   lint` чист.
3. `c17825e` test(a11y) — axe-тесты на `PivotTable`/`ChartCard` (компоненты с реальными находками,
   не только `TableBuilderPanel`, у которого нарушений не было); в `test/setup.ts` задокументирован
   blind spot color-contrast в jsdom.
4. `0ff5b47` fix(db) — `DB_ECHO`/`DB_POOL_SIZE`/`DB_MAX_OVERFLOW` вынесены в `app/config.py` как
   конфигурируемые через `.env`, дефолты дословно воспроизводят прежнее поведение (ничего не меняется
   без явного override).
5. `98433db` fix(load) — исправлен параметр в k6-скрипте (`search=` вместо `q=`), README переписан:
   старые цифры не заменены новыми молча, а ретрагированы с объяснением причины (методологический
   разбор, а не косметика). Методология проверена end-to-end на изолированном throwaway Postgres
   (500 синтетических статей, `DB_ECHO=false`, `pool_size=20`) — 5 VU/10с, все пороги пройдены
   (`p95=216мс`, `0%` ошибок). Прогон явно помечен как smoke-проверка механизма, не как публикуемый
   baseline — 500 строк на 5 VU не сопоставимы с реальным корпусом.

Сквозная проверка с нуля (`npm ci` → typecheck → lint → build → `vitest run` — 639/639, плюс
ruff/mypy/`pytest -m "not requires_pg"` на бэкенде) — всё зелёное, ровно как видит CI.

**Реальный нагрузочный прогон против прод-масштаба данных (2026-07-09).** По просьбе владельца —
одноразовое read-only копирование `articles`+`catalog_articles` (142 658 / 141 878 строк) из прод-Supabase
(`btmiovdmasqufufyuokx`) в тот же изолированный throwaway-контейнер (`asyncpg.copy_from_table`/
`copy_to_table`, ~44 МБ, без единой таблицы users/auth — только публичные метаданные статей), синхронизация
PK-последовательностей, `ANALYZE`. Полный сценарий `baseline.js` (10s ramp → 30s hold → 10s ramp-down,
20 VU) против бэкенда с `DB_ECHO=false`/`pool_size=20`:

*   **Результат:** пороги не пройдены — `P(95)=11.89с`, `P(99)=13.39с`, но **0% ошибок** (не тайм-ауты —
    чистая очередь на CPU). В отличие от первой попытки Agy — это уже не артефакт замера, а настоящий,
    воспроизводимый предел масштабирования.
*   **Root cause, подтверждено `EXPLAIN ANALYZE`:** ни один из двух эндпоинтов не имеет индекса под
    фактическую форму запроса — оба скатываются в parallel seq scan по 142k строк.
    - `/articles/?search=` — `title.ilike(...) OR author.ilike(...)` с ведущим wildcard'ом: не
      использует ни один btree, включая существующие `ix_articles_lower_*` (те — под точное
      совпадение через `lower()`, не под `LIKE '%...%'`). Нужен `pg_trgm` GIN. COUNT(*) для
      пагинации — 284мс/запрос сам по себе (без `LIMIT`, не может прерваться раньше).
    - `/stats/journal-impact` — `EXTRACT(year FROM publication_date) <= max_year`: функция над
      колонкой тоже не индексируется штатно; 112мс/запрос (seq scan + join с `catalog_articles`).
    - По отдельности — доли секунды, незаметно живому пользователю; на 20 параллельных VU все
      параллельные воркеры конкурируют за 4 ядра контейнера — отсюда многосекундный хвост.
*   **Осознанно не фиксили в рамках этого тикета** — добавление `pg_trgm`/функционального индекса
    меняет то, как работает поиск (новая alembic-миграция на прод), это решение для отдельного тикета
    (кандидат в следующий), не drive-by правка при закрытии «измерить и задокументировать».
*   Throwaway-контейнер по завершении возвращён в исходное пустое состояние (`drop_all` +
    `DROP TABLE alembic_version`).

Итог задокументирован в README.md/README.ru.md (секция Performance) — реальные P95/P99 с root cause,
без плейсхолдеров и без выдуманных «хороших» цифр.

**Статус:** пункт 5 (A11y-автоматизация) закрыт, все 639 фронтенд-тестов и полный бэкенд-чеклист
зелёные. Пункт 4 (история производительности) — измерено честно на масштабе, близком к продакшену;
результат неидеальный, но это и есть ценность «военной истории» для README, о которой просил исходный
фидбэк. Найденный индексный пробел (`pg_trgm`/функциональный индекс под year-фильтр) — кандидат в
следующий тикет, отдельно от этого.

### Индексирование БД под нагрузку — ТЗ (2026-07-09, ред. после обсуждения альтернатив)

Root cause (прогон выше): оба эндпоинта — parallel seq scan по 142k строк. Пересмотрено по
итогам обсуждения trade-off'ов GIN (место/скорость записи/pending-buffer): вместо одного
тяжёлого индекса — последовательность шагов от дешёвого к дорогому, с измерением
(`tests/load/baseline.js`, 20 VU, тот же прод-масштабный снапшот) после каждого; следующий шаг
применяется только если предыдущий не довёл до порога `P95<500ms`/`P99<1000ms`.

#### Шаг 1. Убрать точный `COUNT(*)` — «N+» вместо точного числа (нулевая стоимость обслуживания)
Настоящий доминирующий виновник — не сам `ILIKE`, а `COUNT` для пагинации (284мс сам по себе,
без early-exit по `LIMIT`). Кап через подзапрос: `SELECT count(*) FROM (... LIMIT N+1) t` —
Postgres прерывает скан, как только нашёл N+1 совпадений, независимо от реальной селективности
термина. Ни новых индексов, ни расширений, ни задач на autovacuum.
1. `ICatalogRepository.get_total_count` → возвращает `(count, is_capped)`; кап = 2000, единая
   константа для всех фильтров (не только `search`) — без спецкейсов в коде.
2. Контракт `PaginatedArticleResponse` пополняется `total_is_capped: bool` — честно, не
   молчаливое искажение: `total` уже показывается юзеру текстом в `ArticleList`, занижать молча
   нельзя.
3. Фронтенд: `articles.resultsCount_capped` (i18next context-suffix, тот же паттерн, что и
   CLDR-плюрализация `_one/_few/_other`) → «2000+ results», en/ru/sr-Latn.
4. **→ Измерить** и сравнить с уже опубликованным `P95=11.89s`/`P99=13.39s`.

#### Шаг 2. Если Шага 1 недостаточно — `pg_trgm` GiST, не GIN
Применяется только если Шаг 1 не закрыл порог. GiST вместо GIN — по итогам обсуждения: меньше
по размеру, дешевле на запись (bulk-апдейты сидера раз в 2ч), не требует pending-buffer/
autovacuum-внимания GIN; цена — чуть медленнее и lossy-чтение (доп. recheck строк на выходе из
индекса). Два отдельных индекса под `OR`-условие (`title`/`author`), не один составной;
`CREATE EXTENSION IF NOT EXISTS pg_trgm` (сверить `list_extensions` на Supabase);
`CREATE INDEX CONCURRENTLY ... USING gist (... gist_trgm_ops)` — вне транзакции
(`op.get_context().autocommit_block()`), иначе build блокирует таблицу на запись на проде.
**→ Измерить** и сравнить с результатом Шага 1.

#### Шаг 3. Year-фильтр — sargable-предикат, не функциональный индекс
Не индексировать `extract(year FROM publication_date)` — переписать запрос в sargable-диапазон:
`publication_date < make_date(max_year + 1, 1, 1)` + обычный `CREATE INDEX CONCURRENTLY` на
`publication_date` без выражения. Планировщик получает Index Range Scan вместо seq scan
бесплатно. **→ Измерить** в том же прогоне, что и предыдущий применённый шаг.

#### Итоговая верификация
`pg_stat_user_indexes` через 1-2 недели прод-трафика — если Шаг 2 применялся, подтвердить
реальное использование индекса (не write-only bloat), прежде чем считать тикет закрытым.

### Индексирование БД — реализация и результаты (2026-07-09, ветка `feat/a11y-load-test`)

Все три шага ТЗ выполнены последовательно, с измерением после каждого на одном и том же
изолированном throwaway Postgres, засеянном прод-масштабом (142 658 статей / 141 878 связей
catalog_articles — одноразовое read-only копирование из прод-Supabase `btmiovdmasqufufyuokx`,
только `articles`+`catalog_articles`, без PII/auth-таблиц). k6-сценарий — 20 VU, полный цикл
10s ramp → 30s hold → 10s ramp-down, без изменений между прогонами.

**Прогрессия (P95 / P99 / единичный запрос узкого места):**

| Этап | P95 | P99 | Комментарий |
|---|---|---|---|
| Первое честное измерение (без исправлений) | 11.89с | 13.39с | 0% ошибок — чистая CPU-очередь, не тайм-ауты |
| Шаг 1: кап COUNT(*) на 2000 + честное "2000+" | 10.03с | 12.36с | COUNT: 284мс → 121мс; сам скан поиска остался узким местом |
| Шаг 2: `pg_trgm` GiST на title/author | 1.74с | 2.37с | COUNT: 121мс → 42мс (Bitmap Index Scan вместо Seq Scan) |
| Шаг 3: sargable year-фильтр + btree на publication_date | **632мс** | **1.06с** | journal-impact: 112мс → 63мс |

Итог: ~19x к P95, ~13x к P99 относительно первого честного измерения. Пороги
(`P95<500ms`/`P99<1000ms`) не пройдены полностью — P99 промахивается на 60мс — но это уже
реальный, измеренный, воспроизводимый результат, а не догадка.

**Коммиты (`feat/a11y-load-test`):**
- Шаг 1: `perf(catalog): Шаг 1 — капнуть точный COUNT(*), честное "N+" вместо числа` —
  `ICatalogRepository.get_total_count` + cap-параметр, `PaginatedArticleResponse.total_is_capped`,
  фронт (`ArticleList`, i18n en/ru/sr-Latn), 3 новых backend-теста, 1 новый frontend-тест.
- Шаг 2: `perf(catalog): Шаг 2 — pg_trgm GiST индексы на title/author` — migration
  `0016_trgm_gist_search_indices` (`CREATE EXTENSION pg_trgm` + 2×`CREATE INDEX CONCURRENTLY
  ... USING gist`), `_MIGRATION_ONLY_INDICES` в `alembic/env.py` (переименовано из
  `_FUNCTIONAL_INDICES`, расширено под GiST/operator-class индексы).
- Шаг 3: `perf(catalog): Шаг 3 — sargable year-фильтр в get_journal_impact` — migration
  `0017_publication_date_index`, индекс декларирован в `Article.__table_args__` (в отличие от
  Шагов 1-2 — обычный плоский btree, structural-сравнение с моделью работает без exclusion).
- Документация: README.md/README.ru.md — секция Performance переписана с честной прогрессией
  всех трёх шагов вместо изначально опубликованных (и впоследствии ретрагированных) чисел
  баганного прогона Agy CLI.

**Проверено на каждом шаге:** ruff/mypy чисто, `pytest -m "not requires_pg"` зелёный (249,
+3 новых теста к бейзлайну), фронтенд — tsc/lint/build чисты, `vitest run` — 640/640 (+1 новый
тест). `requires_pg` suite (21 тест) и `alembic check` прогнаны в самом конце — throwaway-
контейнер сбрасывается в пустое состояние после переноса прод-данных, эти тесты используют
собственные фикстуры, не прод-копию.

**Осознанно не сделано в рамках этой сессии:**
- Не гонялся `pg_stat_user_indexes`-мониторинг из плана верификации ТЗ — требует 1-2 недели
  реального прод-трафика, разовая задача на будущее, не блокирует закрытие тикета (та же
  логика, что p95-latency-из-логов в Observability, issue #48).
- Замеченный, но не исследованный дальше нюанс: параллельные PG-воркеры (`max_parallel_workers_
  per_gather`) под высокой конкурентностью на CPU-ограниченном контейнере — потенциальный
  дополнительный источник латентности сверх индексации, не входил в согласованный план (Шаги
  1-3), кандидат в отдельное обсуждение, если понадобится закрыть оставшиеся ~130мс до порога.

## Статус выполнения — пункты 4 и 5 (закрыто)

**Смёрджено:** PR #58 (`feat/a11y-load-test` → `main`), merge commit `f37da78`, 2026-07-09.
Диапазон коммитов: `c4c04f7` (Agy CLI, начало ветки) → `44c1067` (мой последний коммит).

**Сделано:**
- Пункт 5 (A11y-автоматизация): `eslint-plugin-jsx-a11y` + `vitest-axe` в существующих
  CI-джобах, без новой джобы; реальные a11y-нарушения найдены и починены.
- Пункт 4 (история производительности): честный k6-baseline на прод-масштабе (142 658 статей),
  root cause найден и исправлен за 3 измеренных шага (`P95` 11.89с → 632мс).
- Восстановление после сломанной первой попытки Agy CLI — все фронтенд CI-джобы и Vercel были
  красными, причина найдена и устранена (не просто перезапущена).
- Миграции `0016`/`0017` применены на staging Supabase (2026-07-09, проверено read-only);
  прод получит их автоматически при следующем деплое (`entrypoint.sh`).

**Вне рамок (осознанно, кандидаты в следующие тикеты):** `pg_trgm`-индекс уже реализован (не
«кандидат», как значилось в исходном ТЗ до пересмотра) — актуальные оставшиеся кандидаты: (1)
`pg_stat_user_indexes`-мониторинг через 1-2 недели прод-трафика, (2) исследование параллельных
PG-воркеров как источника оставшихся ~130мс до порога `P99<1000ms`, (3) `X-Request-ID` на
фронтенде (унаследовано из issue #48, не относится к этой ветке).

Полная хронология, критический разбор подхода Agy CLI и все промежуточные замеры — выше по
документу.
