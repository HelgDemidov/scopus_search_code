// Геометрия сигнатурного эффекта error-страниц (docs/error-experience/spec.md).
// Абсолютные единицы (см) из ТЗ трактуются как относительные — браузер не
// даёт надёжного физического PPI, поэтому длина дуги задаётся в долях
// орбиты чёрной дыры, что даёт то же визуальное соотношение на любом экране.

export const BLACK_HOLE_DIAMETER_RATIO = 0.03; // диаметр = 3% диагонали экрана

// Пороги — в диаметрах чёрной дыры, отсчёт от ПОВЕРХНОСТИ (не от центра)
// Три зоны, не две: FADE_START→OUTER — деформация плавно НАРАСТАЕТ от нуля
// (иначе на границе OUTER был бы разрыв «было 0 — стало 50%/300%» — незаметно
// для статичных звёзд, но критично для курсора, который пересекает границу
// в реальном времени, см. docs/error-experience/spec.md про непрерывность)
export const LENSING_FADE_START_DIAMETERS = 3.0; // дальше — совсем без искажения
export const LENSING_OUTER_DIAMETERS = 1.5; // калибровочная точка ТЗ: −50%/+300%
export const LENSING_INNER_DIAMETERS = 0.5; // здесь и ближе: схлопывание в дугу/полосу

// Угловой охват дуги захваченного объекта (доля окружности орбиты) —
// замена «1–1.5см»/«2–3см» из ТЗ, см. комментарий выше
export const CAPTURED_METEOR_ARC_SPAN = 0.09; // ~2–3см на 15" экране
export const CAPTURED_CURSOR_ARC_SPAN = 0.09;
export const CAPTURED_STAR_ARC_SPAN = 0.045; // ~1–1.5см — вдвое короче метеора/курсора

export const CAPTURED_METEOR_FADE_MS = 250; // как долго видна дуга схлопнувшегося метеора
