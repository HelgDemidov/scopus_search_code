[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths =
    tests
markers =
    manual: requires real API key and external HTTP calls; do not run in CI
    integration: marks tests as integration tests (run with -m integration)
    