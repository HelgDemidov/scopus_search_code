[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths =
    tests
    tools/solid_verifier/tests
markers =
    integration: marks tests as integration tests (run with -m integration)